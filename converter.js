// const csv2json = require('csv2json');
// const fs = require('fs-extra');

// //	change file direction below for desired converted file
// const source = fs.createReadStream(__dirname + '/uploads/stations.csv');
// const output = fs.createWriteStream(__dirname + '/data/stations.json');
// source.pipe(csv2json()).pipe(output);
